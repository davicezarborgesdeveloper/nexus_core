name: clean_module
description: A new Flutter module with Clean Architecture.
version: 0.1.0+1
environment:
  mason: ">=0.1.0 <1.0.0"
vars:
  module_name:
    type: string
    description: "The name of the module (e.g. login, sign_up)"
    default: "my_module"
